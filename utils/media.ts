export const mediaSize = {
  laptopBreakpoint: 768,
};

export const device = {
  mobile: `(max-width: ${mediaSize.laptopBreakpoint}px)`,
  laptop: `(min-width: ${mediaSize.laptopBreakpoint + 1}px)`,
};
